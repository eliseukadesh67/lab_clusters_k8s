version: '3.8'
services:
  server-a:
    build:
      context: .
      dockerfile: services/server-a/Dockerfile

  gateway:
    build:
      context: .
      dockerfile: gateway/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - GRPC_HOST=server-a
      - PROTO_DIR=/app/proto